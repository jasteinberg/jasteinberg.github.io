source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "minima", "~> 2.5"
gem "webrick"   # required by jekyll serve on Ruby >= 3

# For exact parity with GitHub Pages' build environment, comment out
# the jekyll line above and use instead:
# gem "github-pages", group: :jekyll_plugins
